# Public keys only — safe in the frontend build
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: unlock admin via https://yoursite.com/?gate=this-exact-value
# Generate: openssl rand -hex 32
# VITE_ADMIN_GATE_SECRET=

# Admin password lives ONLY in Supabase Auth (Dashboard → Users), not here.
